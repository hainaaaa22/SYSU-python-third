# 人机协作开发报告
## 曾海诚-25361019-第三次人工智能编程作业

---

## 1. 任务拆解与 AI 协作策略
本次公交 IC 卡数据分析作业包含 6 项任务，我采用**逐项交付、分步验收、严格规范**的人机协作策略完成开发：
1. 将作业按任务 1 至任务 6 依次拆解，每次仅向 AI 提出一个任务的需求，保证逻辑清晰、便于排查问题。
2. 明确要求 AI 遵守作业全部约束：必须使用指定库（numpy、pandas、matplotlib、seaborn）、固定函数签名、输出指定图片与文件。
3. 每项任务完成后，运行 AI 提供的独立检验代码确认结果正确，再删除检验代码进入下一任务，保证代码干净可提交。
4. 遇到报错直接将完整报错信息发给 AI，由 AI 定位原因并给出可直接运行的修复代码，全程不改动核心结构。

---

## 2. 核心 Prompt 迭代记录

### 初代 Prompt
帮我写公交 IC 数据分析的代码，完成数据处理、画图、统计、导出和热力图。

### AI 生成的问题
1. 数据读取时分隔符错误，导致列无法识别，出现 `KeyError`。
2. 绘图语法与新版 seaborn 不兼容，出现警告与报错。
3. 未按任务分段，代码混乱，不方便检查与提交。

### 优化后的 Prompt
请按作业 6 个任务分步写代码，每个任务独立可运行；任务 2 必须用 numpy 和 matplotlib，任务 3 函数签名不能改，任务 4 按公式计算 PHF；每项任务后面加一段可单独删除的检验代码，用于检查结果；出现报错请直接修复，保证不影响后续任务。

---

## 3. Debug 记录

### 报错现象
```
KeyError: '交易时间'
```

### 解决过程
1. 根据报错判断为列名不存在或文件读取异常。
2. 让 AI 打印列名，发现所有列被合并成一列，说明分隔符设置错误。
3. AI 将读取方式从 `sep='\t'` 改为 `sep=','`，文件正确分割，列名正常识别，报错解决。

---

## 4. 人工代码审查（逐行中文注释）
以下为任务 4 高峰小时系数核心代码及逐行注释：

```python
# 筛选上车刷卡数据，保证只统计有效上车记录
df_board = df[df['刷卡类型'] == 0].copy()

# 按小时统计刷卡量并排序，得到每小时客流量
hourly_count = df_board['hour'].value_counts().sort_index()

# 找出客流量最大的小时，作为高峰小时
peak_hour = hourly_count.idxmax()

# 高峰小时的总客流量
peak_volume = hourly_count.max()

# 筛选出高峰小时内的所有交易记录
df_peak = df_board[df_board['hour'] == peak_hour].copy()

# 将交易时间列转为标准时间格式
df_peak['交易时间'] = pd.to_datetime(df_peak['交易时间'])

# 按5分钟为一个窗口进行时间向下取整，用于统计5分钟流量
df_peak['time_5min'] = df_peak['交易时间'].dt.floor('5min')

# 找出5分钟窗口内的最大客流量
max_5min = df_peak['time_5min'].value_counts().max()

# 按15分钟为一个窗口进行时间向下取整
df_peak['time_15min'] = df_peak['交易时间'].dt.floor('15min')

# 找出15分钟窗口内的最大客流量
max_15min = df_peak['time_15min'].value_counts().max()

# 按公式计算5分钟高峰小时系数 PHF5
PHF5 = peak_volume / (12 * max_5min)

# 按公式计算15分钟高峰小时系数 PHF15
PHF15 = peak_volume / (4 * max_15min)
```

---
